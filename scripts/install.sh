#!/bin/bash
# Install dependencies and set up the environment for the project
# Check if the script is run as root

pip install -r requirements.txt
echo "All dependencies have been installed successfully."