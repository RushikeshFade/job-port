name: Workflow Practice

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    
jobs: 
  linux_practise:
    runs-on: ubuntu-Latest

    steps:
      - name: checkout
        run: ls -la
      - name: checkout code
        uses: actions/checkout@v3
      - name: list files
        run: ls -la

 

       
